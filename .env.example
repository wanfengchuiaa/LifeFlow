# Optional JSON fortune endpoint. Keep API keys on a server-side proxy.
VITE_FORTUNE_API_URL=
# Leave this as /api for same-origin production. Vite proxies /api to localhost:8080 during development.
VITE_API_BASE_URL=/api
