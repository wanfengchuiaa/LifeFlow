{
  "name": "lifeflow",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "cap:sync": "pnpm build && cap sync",
    "cap:add:android": "cap add android",
    "cap:add:ios": "cap add ios",
    "cap:android": "pnpm build && cap sync android",
    "cap:ios": "pnpm build && cap sync ios",
    "cap:open:android": "cap open android",
    "cap:open:ios": "cap open ios",
    "cap:build:android": "pnpm build && cap sync android && cap build android",
    "cap:build:ios": "pnpm build && cap sync ios && cap build ios"
  },
  "dependencies": {
    "@capacitor/android": "7",
    "@capacitor/core": "7",
    "@capacitor/ios": "7",
    "dexie": "^4.2.0",
    "echarts": "^6.0.0",
    "gsap": "^3.15.0",
    "lucide-vue-next": "^0.468.0",
    "pinia": "^3.0.0",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "7",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.0.0",
    "@vitejs/plugin-vue": "^6.0.0",
    "@vue/test-utils": "^2.4.6",
    "happy-dom": "^18.0.0",
    "typescript": "~5.9.0",
    "vite": "^7.1.0",
    "vite-plugin-pwa": "^1.0.0",
    "vitest": "^3.2.0",
    "vue-tsc": "^3.0.0"
  }
}
